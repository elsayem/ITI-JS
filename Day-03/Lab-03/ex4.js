let operation = (prompt('Enter the operation : Search / Add'))
console.log(operation)
while(operation !== 'search' ){
    // alert('Enter One of the two operations Serach or Add')
    operation = (prompt('Enter the operation : Search / Add')).toLowerCase
}

// operation!== 'add'